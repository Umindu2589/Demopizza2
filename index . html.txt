<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Demo Pizza | Artisanal Slices, Delivered Hot</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;700;800&family=Playfair+Display:wght@700&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --charcoal: #1a1a1a;
            --dark-charcoal: #0f0f0f;
            --tomato-red: #E74C3C;
            --tomato-hover: #C0392B;
            --warm-white: #F5F5F0;
            --gray-text: #B0B0B0;
            --card-bg: #252525;
        }

        html {
            scroll-behavior: smooth;
        }

        body {
            font-family: 'Montserrat', sans-serif;
            background-color: var(--charcoal);
            color: var(--warm-white);
            line-height: 1.6;
            overflow-x: hidden;
        }

        /* Navigation */
        header {
            position: fixed;
            top: 0;
            width: 100%;
            background: rgba(26, 26, 26, 0.95);
            backdrop-filter: blur(10px);
            z-index: 1000;
            padding: 1rem 0;
            transition: all 0.3s ease;
        }

        header.scrolled {
            box-shadow: 0 2px 20px rgba(0,0,0,0.3);
        }

        nav {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo {
            font-family: 'Playfair Display', serif;
            font-size: 1.8rem;
            font-weight: 700;
            color: var(--warm-white);
            text-decoration: none;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .logo-icon {
            color: var(--tomato-red);
            font-size: 2rem;
        }

        .nav-links {
            display: flex;
            list-style: none;
            gap: 2.5rem;
            align-items: center;
        }

        .nav-links a {
            color: var(--warm-white);
            text-decoration: none;
            font-weight: 600;
            font-size: 0.95rem;
            transition: color 0.3s ease;
            position: relative;
        }

        .nav-links a::after {
            content: '';
            position: absolute;
            bottom: -5px;
            left: 0;
            width: 0;
            height: 2px;
            background: var(--tomato-red);
            transition: width 0.3s ease;
        }

        .nav-links a:hover::after {
            width: 100%;
        }

        .nav-links a:hover {
            color: var(--tomato-red);
        }

        .cta-button {
            background: var(--tomato-red);
            color: white;
            padding: 0.8rem 1.5rem;
            border-radius: 50px;
            font-weight: 700;
            transition: all 0.3s ease;
            border: 2px solid var(--tomato-red);
        }

        .cta-button:hover {
            background: transparent;
            color: var(--tomato-red);
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(231, 76, 60, 0.3);
        }

        .hamburger {
            display: none;
            flex-direction: column;
            cursor: pointer;
            gap: 5px;
        }

        .hamburger span {
            width: 25px;
            height: 3px;
            background: var(--warm-white);
            transition: all 0.3s ease;
            border-radius: 3px;
        }

        /* Hero Section */
        .hero {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background: linear-gradient(135deg, rgba(15,15,15,0.9) 0%, rgba(26,26,26,0.8) 100%), 
                        url('https://kimi-web-img.moonshot.cn/img/media.istockphoto.com/d62c25e4fcfdeb75111bdc961bcad9e82fc197d0.jpg');
            background-size: cover;
            background-position: center;
            position: relative;
            padding: 2rem;
        }

        .hero-content {
            text-align: center;
            max-width: 800px;
            animation: fadeInUp 1s ease;
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .hero h1 {
            font-family: 'Playfair Display', serif;
            font-size: clamp(2.5rem, 6vw, 4.5rem);
            margin-bottom: 1.5rem;
            line-height: 1.2;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.5);
        }

        .hero h1 span {
            color: var(--tomato-red);
            display: block;
        }

        .hero-subtitle {
            font-size: 1.2rem;
            color: var(--gray-text);
            margin-bottom: 2.5rem;
            max-width: 600px;
            margin-left: auto;
            margin-right: auto;
        }

        .hero-buttons {
            display: flex;
            gap: 1rem;
            justify-content: center;
            flex-wrap: wrap;
        }

        .btn-primary {
            background: var(--tomato-red);
            color: white;
            padding: 1rem 2.5rem;
            border-radius: 50px;
            text-decoration: none;
            font-weight: 700;
            font-size: 1.1rem;
            transition: all 0.3s ease;
            display: inline-block;
            border: 2px solid var(--tomato-red);
        }

        .btn-primary:hover {
            background: transparent;
            color: var(--tomato-red);
            transform: translateY(-3px);
            box-shadow: 0 10px 30px rgba(231, 76, 60, 0.4);
        }

        .btn-secondary {
            background: transparent;
            color: var(--warm-white);
            padding: 1rem 2.5rem;
            border-radius: 50px;
            text-decoration: none;
            font-weight: 700;
            font-size: 1.1rem;
            transition: all 0.3s ease;
            display: inline-block;
            border: 2px solid var(--warm-white);
        }

        .btn-secondary:hover {
            background: var(--warm-white);
            color: var(--charcoal);
            transform: translateY(-3px);
        }

        .scroll-indicator {
            position: absolute;
            bottom: 2rem;
            left: 50%;
            transform: translateX(-50%);
            animation: bounce 2s infinite;
        }

        @keyframes bounce {
            0%, 20%, 50%, 80%, 100% { transform: translateX(-50%) translateY(0); }
            40% { transform: translateX(-50%) translateY(-10px); }
            60% { transform: translateX(-50%) translateY(-5px); }
        }

        .scroll-indicator svg {
            width: 30px;
            height: 30px;
            fill: var(--warm-white);
            opacity: 0.7;
        }

        /* Menu Section */
        .menu-section {
            padding: 6rem 2rem;
            max-width: 1200px;
            margin: 0 auto;
        }

        .section-header {
            text-align: center;
            margin-bottom: 4rem;
        }

        .section-header h2 {
            font-family: 'Playfair Display', serif;
            font-size: 2.5rem;
            margin-bottom: 1rem;
            position: relative;
            display: inline-block;
        }

        .section-header h2::after {
            content: '';
            position: absolute;
            bottom: -10px;
            left: 50%;
            transform: translateX(-50%);
            width: 60px;
            height: 3px;
            background: var(--tomato-red);
        }

        .section-header p {
            color: var(--gray-text);
            font-size: 1.1rem;
            max-width: 600px;
            margin: 1.5rem auto 0;
        }

        .menu-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2rem;
        }

        .menu-card {
            background: var(--card-bg);
            border-radius: 15px;
            overflow: hidden;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            border: 1px solid #333;
        }

        .menu-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 20px 40px rgba(0,0,0,0.4);
        }

        .menu-card-image {
            height: 250px;
            background-size: cover;
            background-position: center;
            position: relative;
        }

        .menu-card-image::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            right: 0;
            height: 50%;
            background: linear-gradient(to top, var(--card-bg), transparent);
        }

        .menu-card-content {
            padding: 1.5rem;
        }

        .menu-card-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 0.5rem;
        }

        .menu-card h3 {
            font-size: 1.3rem;
            font-weight: 700;
        }

        .price {
            color: var(--tomato-red);
            font-size: 1.4rem;
            font-weight: 800;
        }

        .menu-card p {
            color: var(--gray-text);
            font-size: 0.95rem;
            margin-bottom: 1.5rem;
            line-height: 1.5;
        }

        .add-to-cart {
            width: 100%;
            background: transparent;
            color: var(--tomato-red);
            border: 2px solid var(--tomato-red);
            padding: 0.8rem;
            border-radius: 8px;
            font-weight: 700;
            cursor: pointer;
            transition: all 0.3s ease;
            font-family: 'Montserrat', sans-serif;
        }

        .add-to-cart:hover {
            background: var(--tomato-red);
            color: white;
        }

        /* About Section */
        .about-section {
            background: var(--dark-charcoal);
            padding: 6rem 2rem;
        }

        .about-container {
            max-width: 1200px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 4rem;
            align-items: center;
        }

        .about-image {
            position: relative;
            border-radius: 15px;
            overflow: hidden;
            box-shadow: 0 20px 40px rgba(0,0,0,0.3);
        }

        .about-image img {
            width: 100%;
            height: auto;
            display: block;
            transition: transform 0.5s ease;
        }

        .about-image:hover img {
            transform: scale(1.05);
        }

        .about-content h2 {
            font-family: 'Playfair Display', serif;
            font-size: 2.5rem;
            margin-bottom: 1.5rem;
            line-height: 1.2;
        }

        .about-content p {
            color: var(--gray-text);
            margin-bottom: 1.5rem;
            font-size: 1.1rem;
            line-height: 1.8;
        }

        .feature-list {
            list-style: none;
            margin-top: 2rem;
        }

        .feature-list li {
            display: flex;
            align-items: center;
            gap: 1rem;
            margin-bottom: 1rem;
            color: var(--warm-white);
        }

        .feature-icon {
            width: 40px;
            height: 40px;
            background: rgba(231, 76, 60, 0.2);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--tomato-red);
            font-size: 1.2rem;
        }

        /* Footer */
        footer {
            background: #0a0a0a;
            padding: 4rem 2rem 2rem;
            border-top: 1px solid #222;
        }

        .footer-content {
            max-width: 1200px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 3rem;
            margin-bottom: 3rem;
        }

        .footer-section h3 {
            font-family: 'Playfair Display', serif;
            font-size: 1.5rem;
            margin-bottom: 1.5rem;
            color: var(--warm-white);
        }

        .footer-section p, .footer-section a {
            color: var(--gray-text);
            text-decoration: none;
            line-height: 1.8;
            transition: color 0.3s ease;
        }

        .footer-section a:hover {
            color: var(--tomato-red);
        }

        .hours-list {
            list-style: none;
        }

        .hours-list li {
            display: flex;
            justify-content: space-between;
            margin-bottom: 0.5rem;
            color: var(--gray-text);
        }

        .social-links {
            display: flex;
            gap: 1rem;
            margin-top: 1rem;
        }

        .social-links a {
            width: 40px;
            height: 40px;
            background: var(--card-bg);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.3s ease;
            border: 1px solid #333;
        }

        .social-links a:hover {
            background: var(--tomato-red);
            border-color: var(--tomato-red);
            transform: translateY(-3px);
        }

        .footer-bottom {
            text-align: center;
            padding-top: 2rem;
            border-top: 1px solid #222;
            color: var(--gray-text);
            font-size: 0.9rem;
        }

        /* Scroll to Top Button */
        .scroll-top {
            position: fixed;
            bottom: 2rem;
            right: 2rem;
            width: 50px;
            height: 50px;
            background: var(--tomato-red);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            opacity: 0;
            visibility: hidden;
            transition: all 0.3s ease;
            z-index: 999;
            border: none;
            box-shadow: 0 4px 15px rgba(231, 76, 60, 0.3);
        }

        .scroll-top.visible {
            opacity: 1;
            visibility: visible;
        }

        .scroll-top:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 25px rgba(231, 76, 60, 0.4);
        }

        .scroll-top svg {
            width: 24px;
            height: 24px;
            fill: white;
        }

        /* Cart Notification */
        .cart-notification {
            position: fixed;
            top: 100px;
            right: 2rem;
            background: var(--tomato-red);
            color: white;
            padding: 1rem 1.5rem;
            border-radius: 8px;
            font-weight: 600;
            transform: translateX(400px);
            transition: transform 0.3s ease;
            z-index: 1001;
            box-shadow: 0 5px 20px rgba(0,0,0,0.3);
        }

        .cart-notification.show {
            transform: translateX(0);
        }

        /* Mobile Responsive */
        @media (max-width: 768px) {
            .hamburger {
                display: flex;
            }

            .nav-links {
                position: fixed;
                top: 70px;
                right: -100%;
                width: 100%;
                height: calc(100vh - 70px);
                background: rgba(26, 26, 26, 0.98);
                flex-direction: column;
                padding: 2rem;
                transition: right 0.3s ease;
                gap: 2rem;
            }

            .nav-links.active {
                right: 0;
            }

            .hero h1 {
                font-size: 2.5rem;
            }

            .about-container {
                grid-template-columns: 1fr;
                gap: 2rem;
            }

            .menu-grid {
                grid-template-columns: 1fr;
            }

            .hero-buttons {
                flex-direction: column;
                width: 100%;
            }

            .btn-primary, .btn-secondary {
                width: 100%;
                text-align: center;
            }
        }

        /* Animations on Scroll */
        .fade-in {
            opacity: 0;
            transform: translateY(30px);
            transition: opacity 0.6s ease, transform 0.6s ease;
        }

        .fade-in.visible {
            opacity: 1;
            transform: translateY(0);
        }
    </style>
<base target="_blank">
</head>
<body>

    <!-- Navigation -->
    <header id="header">
        <nav>
            <a href="#" class="logo">
                <span class="logo-icon">🍕</span>
                Demo Pizza
            </a>
            <ul class="nav-links" id="navLinks">
                <li><a href="#home">Home</a></li>
                <li><a href="#menu">Menu</a></li>
                <li><a href="#about">About</a></li>
                <li><a href="#contact" class="cta-button">Order Now</a></li>
            </ul>
            <div class="hamburger" id="hamburger">
                <span></span>
                <span></span>
                <span></span>
            </div>
        </nav>
    </header>

    <!-- Hero Section -->
    <section class="hero" id="home">
        <div class="hero-content">
            <h1>Artisanal Slices, <span>Delivered Hot</span></h1>
            <p class="hero-subtitle">Experience the perfect fusion of traditional wood-fired techniques and modern flavors. Every pizza is handcrafted with passion in our stone ovens.</p>
            <div class="hero-buttons">
                <a href="#menu" class="btn-primary">View Menu</a>
                <a href="#contact" class="btn-secondary">Find Us</a>
            </div>
        </div>
        <div class="scroll-indicator">
            <svg viewBox="0 0 24 24"><path d="M7.41 8.59L12 13.17l4.59-4.58L18 10l-6 6-6-6 1.41-1.41z"/></svg>
        </div>
    </section>

    <!-- Menu Section -->
    <section class="menu-section" id="menu">
        <div class="section-header fade-in">
            <h2>Our Signature Pizzas</h2>
            <p>Handcrafted with premium ingredients and baked to perfection in our wood-fired ovens at 900°F</p>
        </div>
        
        <div class="menu-grid">
            <!-- Margherita -->
            <div class="menu-card fade-in">
                <div class="menu-card-image" style="background-image: url('https://kimi-web-img.moonshot.cn/img/thumbs.dreamstime.com/f99b80b489090d1949c80498c7142dd2e5da26da.jpg');">
                </div>
                <div class="menu-card-content">
                    <div class="menu-card-header">
                        <h3>The Classic Margherita</h3>
                        <span class="price">$14</span>
                    </div>
                    <p>San Marzano tomato sauce, fresh mozzarella di bufala, basil leaves, and extra virgin olive oil on our signature thin crust.</p>
                    <button class="add-to-cart" onclick="addToCart('The Classic Margherita')">Add to Cart</button>
                </div>
            </div>

            <!-- Pepperoni -->
            <div class="menu-card fade-in">
                <div class="menu-card-image" style="background-image: url('https://kimi-web-img.moonshot.cn/img/img.freepik.com/bf2550e65b143c1cf27bb1380eaaff4515e6edfc.jpg');">
                </div>
                <div class="menu-card-content">
                    <div class="menu-card-header">
                        <h3>Spicy Pepperoni</h3>
                        <span class="price">$16</span>
                    </div>
                    <p>Double pepperoni, jalapeños, corn, mozzarella, and our secret spicy tomato sauce with a drizzle of hot honey.</p>
                    <button class="add-to-cart" onclick="addToCart('Spicy Pepperoni')">Add to Cart</button>
                </div>
            </div>

            <!-- Veggie -->
            <div class="menu-card fade-in">
                <div class="menu-card-image" style="background-image: url('https://kimi-web-img.moonshot.cn/img/c8.alamy.com/3a91125eab6f8b4b7cd105da2784343d1c304137.jpg');">
                </div>
                <div class="menu-card-content">
                    <div class="menu-card-header">
                        <h3>Veggie Garden</h3>
                        <span class="price">$15</span>
                    </div>
                    <p>Roasted peppers, artichokes, olives, mushrooms, red onions, and fresh arugula with garlic herb oil.</p>
                    <button class="add-to-cart" onclick="addToCart('Veggie Garden')">Add to Cart</button>
                </div>
            </div>
        </div>
    </section>

    <!-- About Section -->
    <section class="about-section" id="about">
        <div class="about-container">
            <div class="about-image fade-in">
                <img src="https://kimi-web-img.moonshot.cn/img/images.stockcake.com/140b8898e7e0c4050a7e2f503c6918a5af784131.jpg" alt="Chef preparing pizza in wood-fired oven">
            </div>
            <div class="about-content fade-in">
                <h2>Crafted by Fire, <span style="color: var(--tomato-red);">Served with Passion</span></h2>
                <p>At Demo Pizza, we believe that great pizza starts with fire. Our imported Italian wood-fired ovens reach temperatures of 900°F, creating the perfect char on our hand-stretched dough while keeping the center soft and airy.</p>
                <p>We source our ingredients from local farms and Italian importers. Our dough is fermented for 72 hours for optimal flavor and digestibility.</p>
                
                <ul class="feature-list">
                    <li>
                        <div class="feature-icon">🔥</div>
                        <span>900°F Wood-Fired Ovens</span>
                    </li>
                    <li>
                        <div class="feature-icon">🌾</div>
                        <span>72-Hour Fermented Dough</span>
                    </li>
                    <li>
                        <div class="feature-icon">🇮🇹</div>
                        <span>Imported Italian Ingredients</span>
                    </li>
                    <li>
                        <div class="feature-icon">♻️</div>
                        <span>Sustainable & Local Sourcing</span>
                    </li>
                </ul>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer id="contact">
        <div class="footer-content">
            <div class="footer-section">
                <h3>Demo Pizza</h3>
                <p>123 Artisan Avenue<br>Downtown District<br>New York, NY 10001</p>
                <p style="margin-top: 1rem;">(555) 123-4567<br>hello@demopizza.com</p>
            </div>
            
            <div class="footer-section">
                <h3>Hours</h3>
                <ul class="hours-list">
                    <li><span>Mon - Thu</span> <span>11am - 10pm</span></li>
                    <li><span>Friday</span> <span>11am - 11pm</span></li>
                    <li><span>Saturday</span> <span>12pm - 11pm</span></li>
                    <li><span>Sunday</span> <span>12pm - 9pm</span></li>
                </ul>
            </div>
            
            <div class="footer-section">
                <h3>Follow Us</h3>
                <p>Stay updated with our latest creations and offers</p>
                <div class="social-links">
                    <a href="#" aria-label="Instagram">📷</a>
                    <a href="#" aria-label="Facebook">📘</a>
                    <a href="#" aria-label="Twitter">🐦</a>
                </div>
            </div>
        </div>
        
        <div class="footer-bottom">
            <p>&copy; 2024 Demo Pizza. All rights reserved. Crafted with ❤️ and 🔥</p>
        </div>
    </footer>

    <!-- Scroll to Top Button -->
    <button class="scroll-top" id="scrollTop" onclick="scrollToTop()">
        <svg viewBox="0 0 24 24"><path d="M7.41 15.41L12 10.83l4.59 4.58L18 14l-6-6-6 6z"/></svg>
    </button>

    <!-- Cart Notification -->
    <div class="cart-notification" id="cartNotification">
        Added to cart! 🛒
    </div>

    <script>
        // Mobile Hamburger Menu
        const hamburger = document.getElementById('hamburger');
        const navLinks = document.getElementById('navLinks');

        hamburger.addEventListener('click', () => {
            navLinks.classList.toggle('active');
            
            // Animate hamburger to X
            const spans = hamburger.querySelectorAll('span');
            if (navLinks.classList.contains('active')) {
                spans[0].style.transform = 'rotate(45deg) translate(5px, 5px)';
                spans[1].style.opacity = '0';
                spans[2].style.transform = 'rotate(-45deg) translate(7px, -6px)';
            } else {
                spans[0].style.transform = 'none';
                spans[1].style.opacity = '1';
                spans[2].style.transform = 'none';
            }
        });

        // Close mobile menu when clicking a link
        document.querySelectorAll('.nav-links a').forEach(link => {
            link.addEventListener('click', () => {
                navLinks.classList.remove('active');
                const spans = hamburger.querySelectorAll('span');
                spans[0].style.transform = 'none';
                spans[1].style.opacity = '1';
                spans[2].style.transform = 'none';
            });
        });

        // Header scroll effect
        const header = document.getElementById('header');
        window.addEventListener('scroll', () => {
            if (window.scrollY > 50) {
                header.classList.add('scrolled');
            } else {
                header.classList.remove('scrolled');
            }
        });

        // Scroll to Top Button visibility
        const scrollTopBtn = document.getElementById('scrollTop');
        window.addEventListener('scroll', () => {
            if (window.scrollY > 500) {
                scrollTopBtn.classList.add('visible');
            } else {
                scrollTopBtn.classList.remove('visible');
            }
        });

        function scrollToTop() {
            window.scrollTo({
                top: 0,
                behavior: 'smooth'
            });
        }

        // Add to Cart functionality
        function addToCart(itemName) {
            const notification = document.getElementById('cartNotification');
            notification.textContent = `${itemName} added to cart! 🛒`;
            notification.classList.add('show');
            
            setTimeout(() => {
                notification.classList.remove('show');
            }, 3000);
        }

        // Intersection Observer for fade-in animations
        const observerOptions = {
            threshold: 0.1,
            rootMargin: '0px 0px -50px 0px'
        };

        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('visible');
                }
            });
        }, observerOptions);

        document.querySelectorAll('.fade-in').forEach(el => {
            observer.observe(el);
        });

        // Smooth scroll for anchor links (fallback for older browsers)
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });
    </script>
</body>
</html>